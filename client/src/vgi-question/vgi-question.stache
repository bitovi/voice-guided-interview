<div>
  <p>{{question}}</p>
  <select {($value)}="answer">
    {{^answer}}
      <option value="">Please select an option</option>
    {{/answer}}

    {{#each options}}
      <option {$value}=".">{{.}}</option>
    {{/each}}
  </select>
  <input type="hidden" {($value)}="answer" />
</div>
